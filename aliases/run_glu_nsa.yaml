---
name: "get_nsa"
pack: "gluware"
action_ref: "gluware.get_nsa"
description: "Runs [g]luware network state assessment on a network device (host)."
formats:
  - display: (exec|execute|start) NSA on {{device_name}}
    representation:
    - (exec|execute|start) NSA on {{device_name}}
ack:
  enabled: true
  format: "Executing `{{ actionalias.ref }}`, your ID is `{{ execution.id[:2] }}..{{ execution.id[-2:] }}`"
  append_url: true 